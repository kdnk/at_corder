#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);
  int x, y;
  cin >> x >> y;
  vector<int> A, B;
  A = {1, 3, 5, 7, 8, 10, 12};
  B = {4, 6, 9, 11};

    return 0;
}

// find(a.begin(), a.end(), x) != a.end()
