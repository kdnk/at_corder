#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);

  int t, x;
  cin >> t >> x;

  cout << fixed << setprecision(10) << ((double)t / (double)x) << endl;

  return 0;
}
