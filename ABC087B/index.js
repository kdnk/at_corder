// @ts-check

const VALUE = {
  a: 500,
  b: 100,
  c: 50
};

function Main(input) {
  input = input.split("\n");
  const a = input[0];
  const b = input[1];
  const c = input[2];
  const x = input[3];
}

const calcX = (a, b, c, x) => {};

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
